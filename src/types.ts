export type option = {
  questionId: string;
  optionId: string;
  optionTitle: string;
  value: string;
};

export type section = {
  sectionId: string;
  sectionName: string;
  sectionDesc: string;
  formId: string;
};